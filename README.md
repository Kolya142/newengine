# NewEngine Studio 🛠️

**NewEngine Studio** — это легковесная среда разработки (IDE) и лаунчер для игрового движка **NewEngine**.

Приложение объединяет в себе систему сборки, управление ассетами и инструменты для быстрой разработки игр на C без необходимости настраивать сложные Makefile или CMake скрипты вручную.


## ✨ Основные возможности

### 🏗️ Умная система сборки
*   **Автоматический поиск файлов:** Студия сама сканирует папки `engine/` и `game/` и находит все исходники.
*   **Многопоточная компиляция:** Интерфейс не зависает во время сборки.
*   **Smart Linking:** Решает проблему множественного определения `main()` путем подмены символов на лету (раздельная компиляция движка и игры).
*   **Кроссплатформенность:** Автоматически подставляет нужные флаги для Windows (MinGW) и Linux (GCC).

### ⚡ Hot Reload (Живая перезагрузка)
*   Включите чекбокс **"⚡ Авто-сборка"**.
*   При сохранении любого `.c` файла в редакторе (VS Code, Notepad++), Студия автоматически перекомпилирует проект и перезапустит игру за секунду.

### 📦 Конвейер Ассетов (Asset Pipeline)
*   **OBJ to Header:** Встроенный конвертер 3D-моделей.
*   Превращает `.obj` файлы в готовые C-структуры (`NE_Vertex`, `NE_Face`), которые можно сразу подключать через `#include`.

### 🔄 Центр обновлений
*   Встроенная система обновлений позволяет в один клик получить свежую версию **Студии** или ядра **Движка** напрямую с GitHub репозиториев разработчиков.

---

## 🚀 Начало работы

### Требования
*   **ОС:** Windows 10/11 или Linux.
*   **Python:** Версия 3.10 или выше.
*   **Компилятор C:** GCC (MinGW для Windows).

### Установка

1.  **Скачайте проект:**
    ```bash
    git clone https://github.com/Kolya142/newengine.git
    cd newengine
    ```

2.  **Установите зависимости Python:**
    ```bash
    pip install customtkinter
    ```

3.  **Настройка GCC (Только для Windows):**
    *   Скачайте [w64devkit](https://github.com/skeeto/w64devkit/releases) или [WinLibs](https://winlibs.com/).
    *   Добавьте путь к папке `bin` в Переменные Среды (Path).
    *   Проверьте в терминале: `gcc --version`.

4.  **Запуск:**
    ```bash
    python studio.py
    ```

---

## 🎮 Как создать игру

1.  Откройте **NewEngine Studio**.
2.  Перейдите во вкладку **"Код & Логи"**.
3.  Исходный код игры находится в файле `game/main.c`.
4.  Нажмите **"🚀 Build & Run"** для первого запуска.

### Пример добавления модели
1.  Перейдите во вкладку **"Ассеты (3D)"**.
2.  Выберите файл `.obj`.
3.  Нажмите **"Сконвертировать"**.
4.  В коде игры подключите модель:
    ```c
    #include <model_name.h>
    // ...
    NScreen_RenderModel(model_name_model, transform);
    ```

---

## 📂 Структура проекта

```text
NEWENGINE-MAIN/
├── bin/                # Сюда сохраняется скомпилированная игра (.exe)
│   └── obj/            # Промежуточные объектные файлы (.o)
├── assets/             # Сгенерированные хедеры моделей (.h)
├── engine/             # Исходный код ядра движка
│   ├── entity.c        # Система сущностей (ECS)
│   ├── screen.c        # Рендеринг и окно (RGFW + OpenGL)
│   └── main.c          # Математика и утилиты
├── game/               # Код вашей игры
│   └── main.c          # Точка входа (main)
├── include/            # Заголовочные файлы (.h)
├── thirdparty/         # Сторонние библиотеки (RGFW.h)
└── studio.py           # Исходный код NewEngine Studio
```
## 🛠️ Технологии

GUI: Python + CustomTkinter.
Windowing & Input: RGFW (Single-header library).
Graphics: Legacy OpenGL (GL 1.x) — эстетика ретро 90-х.
Build System: GCC Wrapper.

## 👥 Авторы и Ссылки

NewEngine Core: Kolya142/newengine
NewEngine Studio: crimbrodev/newengineSTUDIO
